export const LANGUAGES = {
  es: 'es',
  en: 'en'
}
export const NAME_LANGUAGES = {
  [LANGUAGES.en]: 'english',
  [LANGUAGES.es]: 'español',
}
export const LANGUAGES_VALUES = Object.values(LANGUAGES);
export const LANGUAGES_KEYS = Object.keys(LANGUAGES);
export const DEFAULT_LANG = LANGUAGES.es
